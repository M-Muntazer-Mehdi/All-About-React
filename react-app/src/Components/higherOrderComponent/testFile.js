import React from "react";

const WrapFile = () => {
    return(
        <div>
            <h4>Hoc Wrapped Component</h4>
            <p>In this, We Wrap the testfile with Higher order component with the use of a login page</p>
        </div>
    );
};

export default WrapFile;