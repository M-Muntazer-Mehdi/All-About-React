import React from "react";
import ControlledForm from "./controlledForm";

function FormMain () {
    return (
        <div>
            <ControlledForm />
        </div>
    );
}

export default FormMain;